<div class="col-lg-3 pt-4 pl-5 bg-light border-start border-end">
      <div class="ms-5">    
        <a href="#" class="logo mb-5 mt-3 text-purple-light">#Tuwaaye</a>
        <div class="post pt-4 pb-2 mt-4">
            <ul class="story_author d-flex flex-column pb-3">
              <li class="big-profile">
                <img src="img/prof.png" alt="">
              </li>
            </ul>
            <div class="d-flex flex-column">
              <p class="font-weight-bold">
                <%= current_user.first_name %> <%= current_user.last_name %>
              </p>
            </div>
        </div>

        <nav class="d-flex flex-column">
            <%= link_to "New Story", new_story_path, class: "mt-3 text-purple-light" %>
            <a href="#" class="mt-3 text-purple-light">Feed</a>
            <a href="#" class="mt-3 text-purple-light">My Account</a>
            <a href="#" class="mt-3 text-purple-light">Messages</a>
            <a href="#" class="mt-3 text-purple-light">Notifications</a>
            <a href="#" class="mt-3 text-purple-light">Communities</a>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "mt-5 text-purple-light" %>
        </nav>
      </div>
    </div>